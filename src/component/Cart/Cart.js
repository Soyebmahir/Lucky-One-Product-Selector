import React from 'react';
import './Cart.css'

const Cart = ({cart}) => {
    console.log(cart);
    return (
        <div className='cart'>
            <h1>hello there</h1>
        </div>
    );
};

export default Cart;